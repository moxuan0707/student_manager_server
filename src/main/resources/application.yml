spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3316/studentms?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 111111

mybatis:
  type-aliases-package: cn.shu.student_server.entity
  mapper-locations: classpath:mapper/*.xml

server:
  port: 10086